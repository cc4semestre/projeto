classDiagram
    class Professor {
        +String nome
        +String id
        +logIn(credenciais)
        +selecionarTurma(idTurma)
        +marcarPresenca(idAluno, status)
        +logOut()
    }

    class Aluno {
        +String nome
        +String id
        +List presencas
        +atualizarPresenca(status)
        +obterPorcentagemPresenca() : float
    }

    class Responsavel {
        +String nome
        +String id
        +List notificacoes
        +receberNotificacao(mensagem)
    }

    class Sistema {
        +List turmas
        +BancoDeDados bd
        +logIn(credenciaisUsuario)
        +exibirTurmas()
        +atualizarPresenca(idAluno, status)
        +calcularPorcentagem()
        +notificarBaixaPresenca(idAluno)
        +logOut(idUsuario)
    }

    class BancoDeDados {
        +List alunos
        +List registrosPresenca
        +verificarCredenciais(idUsuario, senha)
        +obterListaAlunos(idTurma)
        +atualizarRegistroPresenca(idAluno, data, status)
        +obterPorcentagemPresenca(idAluno)
    }

    class SistemaDeNotificacao {
        +enviarNotificacao(idAluno, mensagem)
    }

    Professor "1" -- "1" Sistema : utiliza >
    Sistema "1" -- "*" BancoDeDados : interage >
    Sistema "1" -- "*" SistemaDeNotificacao : envia notificações >
    Aluno "*" -- "1" Turma : frequenta >
    Responsavel "1" -- "*" SistemaDeNotificacao : recebe notificações >
